"""
Mock network operations for SpiderFoot tests.
"""
from unittest.mock import patch, Mock

# Example usage in tests:
# with patch('requests.get') as mock_get:
#     mock_get.return_value = Mock(status_code=200)
